import styled from 'styled-components';

export const Wrapper = styled.div`
  height: 100%;
  padding-bottom: 20px;
  overflow: auto;

  background: #000;
  background: linear-gradient(180deg, #fff, #0473bb);
`;
